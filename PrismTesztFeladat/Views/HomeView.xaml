<UserControl x:Class="PrismTesztFeladat.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="20" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" VerticalAlignment="Top" Text="Home" FontSize="32"/>
        <Button Command="{Binding ShowModulesCommand}" Grid.Row="1" VerticalAlignment="Top" Content="Change view" />
    </Grid>
</UserControl>
